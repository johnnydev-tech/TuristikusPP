<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.johnsjohnny.turistikus">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AnuncianteActivity"></activity>
        <activity android:name=".VisitanteActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".PerfilActivity" />
        <activity android:name=".AnunciosActivity" />
        <activity android:name=".GuiaActivity" />
        <activity
            android:name=".Activity.Activity.ListaTelaActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".InicioActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>